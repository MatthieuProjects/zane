{
  "name": "games",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "packages/gems",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tl build --build-dir dist",
        "outputPath": "dist/packages/games"
      }
    }
  }
}
